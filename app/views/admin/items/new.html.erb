<div class="container">
  <div class="row">
    <div class="col-md-10 mx-auto">

      <div class="mt-2 mb-4 bg-light d-inline-block">
        <h4 class="px-2 pt-2">商品新規登録</h4>
      </div>

      <%= form_with model: @item, url: admin_items_path, local: true do |f| %>
        <div class="row mx-2">
          <div class="col-lg-8 col-md-10 mx-auto">

            <div class="form-group row">
              <p class="col-md-4">商品画像</p>
              <%= f.attachment_field :image, class: "form-control-file col-md-8" %>
             </div>

            <div class="form-group row">
              <p class="col-md-4 pt-1">商品名</p>
              <%= f.text_field :name, placeholder:"商品名", class: "form-control col-md-8" %>
            </div>

            <div class="form-group row">
              <p class="col-md-4 pt-1">商品説明</p>
              <%= f.text_area :introduction, placeholder:"ここに説明を記述します", class: "form-control col-md-8" %>
            </div>

            <div class="form-group row">
              <p class="col-md-4 pt-1">ジャンル</p>
              <%= f.collection_select(:genre_id, @genres, :id, :name, {prompt: "--選択してください--"}, {class: "form-control col-md-8"}) %>
            </div>

            <div class="form-group row">
              <p class="col-md-4 pt-1">税抜価格</p>
              <%= f.number_field :price, placeholder:"1000", class: "form-control col-md-7", min: 1 %>
              <%= f.label :price, "円", class: "form-control col-md-1" %>
            </div>

            <div class="form-group row">
              <p class="col-md-4 pt-1">販売ステータス</p>
              <div class="col-md-8">
                <%= f.radio_button :is_active, :true %>
                <%= f.label :is_active, "販売中", {value: :true, style: "display: inline-block;"} %>
                <%= f.radio_button :is_active, :false %>
                <%= f.label :is_active, "販売停止中", {value: :false, style: "display: inline-block;"} %>
              </div>
            </div>

          </div>
        </div>

        <div class="text-center pt-4">
          <%= f.submit "新規登録", class:"btn btn-success px-5" %>
        </div>
      <% end %>

    </div>
  </div>
</div>