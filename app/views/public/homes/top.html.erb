<div class="container" style="min-width: 430px;">

  <div class="row">
    <div class="col-lg-9 pt-4 pb-5 px-3 mx-auto">
      <div class="main-visual row m-0 d-flex justify-content-center align-items-center">
        <div class="main-visual-content col-10 pt-2 text-center text-nowrap">
          <h4>ようこそ、ながのCAKEへ!</h4>
          <p>
            ケーキ販売のECサイトになります。<br>
            会員でない⽅も商品の閲覧はできますが、<br>
            購⼊には会員登録が必要になります。<br>
          </p>
        </div>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-lg-10 mx-auto">
      <h5><strong>新着商品</strong></h5>

      <div class="row">
        <% @items.each do |item| %>
          <div class="col-6 col-md-3 px-2 pb-3">
            <div class="card">
              <%= link_to item_path(item.id) do %>
                <%= attachment_image_tag item, :image, size: "400x300", format: "jpg", class: "card-img-top img-fluid" %>
              <% end %>
              <%= link_to item_path(item.id), class: "text-dark" do %>
                <div class="card-body">
                  <h6 class="card-title text-truncate"><%= item.name %></h6>
                  <h6 class="card-text">￥<%= item.price.to_s(:delimited) %></h6>
                </div>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
      <div class="text-right mb-5">
        <%= link_to "すべての商品を見る>", items_path, class: "text-dark" %>
      </div>
    </div>
  </div>

</div>